# 环境配置示例文件
# 复制此文件到 .env.development（开发环境）或 .env.production（生产环境）

# 服务器配置
PORT=3002                   # 服务器端口
NODE_ENV=development        # 环境类型：development 或 production

# 数据库配置
DATABASE_URL="mongodb+srv://username:password@your-mongodb-url/database-name?retryWrites=true&w=majority"

# JWT 配置
JWT_SECRET=your-jwt-secret  # JWT 密钥，生产环境应使用复杂的随机字符串
JWT_EXPIRES_IN=7d          # JWT 过期时间

# CORS 配置
CORS_ORIGIN=http://localhost:3000  # 允许的前端源
                                  # 开发环境：http://localhost:3000
                                  # 生产环境：https://your-domain.com 